.mint-tab-item {
  display flex
  flex-direction column
  align-items center
  justify-content center
}
.mint-header {
  background #FFFFFF
  font-size: 1rem;
  color: #333333;
  text-align: center;
}
